@{
    ViewData["Title"] = "Upo e-Commerce - REGISTRAZIONE";
    var ruolo = HttpContextAccessor.HttpContext.Session.GetString("Role");
}

<script type="text/javascript" src="~/js/validate.js"></script>

<div class="container">
    <div class="main">

        <!-- Controllo ruoli: ci si puo' registrare solo se non si e' gia' loggati -->
        @if (ruolo != null)
        {
            <h1>Sei gi&aacute; registrato!</h1>
        }
        else
        {
         

            <h1>
                <!-- MESSAGGIO possibile login errata -->
                @TempData["LoginMsg"]

            </h1>

            <!-- Form registrazione -->
            <div class="login__pagediv">
                @using (Html.BeginForm("Create", "User", FormMethod.Post, new { @id = "form1" }))
                {
                    <fieldset>
                        Username o e-mail
                        <br />
                        <input type="text" name="user"  class="casella__user" id="user" required />
                        <br />
                        <br />
                        Password
                        <br />
                        <input type="password" name="pass"  class="casella__user" id="pass" required />
                        <br />
                        <br />
                        Conferma Password
                        <br />
                        <input type="password" name="pass2" id="pass2"  class="casella__user" required />
                        <br />
                        <br />
                        <br />
                        <input type="submit" class="botton__submit" onclick="return validateUser();" value="Registra" />
                    </fieldset>
                }
            </div>
        }
    </div>

    <footer class="footer">
      
    </footer>
</div>